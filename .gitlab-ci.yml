variables:
  CI_DOCKERHUB_IMAGE: huncrys/netdata-unlock-5-nodes
  CI_GHCR_IMAGE: huncrys/netdata-unlock-5-nodes

include:
  - component: $CI_SERVER_FQDN/oaklab/ci-templates/hadolint@10
  - component: $CI_SERVER_FQDN/oaklab/ci-templates/oci-publish@10
    inputs:
      licenses: MIT
      platforms:
        - linux/amd64
        - linux/arm64
